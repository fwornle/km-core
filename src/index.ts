// PLACEHOLDER - Plan 02 replaces this with the real public API barrel.
// Wave 0 ships only this version constant so `npm run build` exits 0
// and the RED test suite can import (and fail) against a real module.

export const KM_CORE_VERSION = '0.1.0-pre';
